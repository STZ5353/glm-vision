# runtime cache (auto-generated by the skill)
.cache/

# API key config and cache files (contain secrets or recognition results)
*.glm-vision.json

# brotli-compressed copies of mupdf (redundant, not needed at runtime)
*.br

# OS junk files
Thumbs.db
.DS_Store
