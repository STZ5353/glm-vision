---
name: glm-vision
description: 视觉之眼：智谱 GLM 免费视觉模型的万能入口（图片/PDF/Word/图片URL/多图对比，跨 Win/macOS/Linux 零安装）。任何涉及"看"的需求都用它：识别图片内容、OCR 提取文字、分析图表/流程图/架构图、解析报错截图、读 PDF 论文/扫描件/合同、提取 Word 文档（纯文本零 API 成本）、对比多张图片差异、识别图片 URL——即使用户没有明说"看图"也要主动想到它。核心脚本 glm-vision.ts（bun 单运行时，零 npm/零 pip/零联网）调用 glm-4.6v-flash（免费，支持深度思考），遇限流自动退避重试并沿免费模型链降级；PDF 由内置 mupdf-wasm 渲染逐页识别，支持断点续跑；Word 走三级降级链（文本解析零成本 → Word COM/LibreOffice 转 PDF → 嵌入图片直抽）。文件需位于工作目录内。
---

# 视觉之眼（glm-vision）Skill v2.1

## 质疑与回应摘要（必选）

### v1.2 既有质疑（v2.0 继续有效，标注 v2 变化）

- 质疑1：图片如何到达 Skill？聊天附件透传不可靠。→ 本地文件路径模式，脚本直接读取。（v2 新增图片 URL 直传，不依赖任何附件机制）
- 质疑2：API Key 硬编码泄露风险。→ Key 永不写入本文件；五级外部来源（`--api-key` → `ZHIPU_API_KEY`/`GLM_API_KEY` → 脚本旁 `.glm-vision.json` → `~/.glm-vision.json`）。
- 质疑3：用户只给指代（"看下这张图"）没有路径。→ 工作流第1步：查工作目录列候选请用户确认，禁止猜测。
- 质疑4：超限/损坏文件直接崩溃。→ 脚本内置预检（存在性→扩展名→0字节→大小→**v2 新增像素尺寸 ≤6000×6000**），超限拦截并给指引；413 有专属文案。
- 质疑5：限流中断识别。→ 429 退避重试 + **v2 新增免费模型自动降级链**（实测有效：429 时自动切 glm-4v-flash 完成识别）；请求带超时杜绝永久挂起。
- 质疑6：隐私文件上传第三方。→ 工作流含隐私自检，敏感内容先征得用户同意。
- 质疑7：主模型二次加工失真。→ 输出强制"识别结果（原样）/ 我的分析"分段隔离。
- 质疑8：误传整个文件夹。→ **v2 已移除数量硬上限**（用户决策"彻底放开"），改为警告+断点续跑对冲（见质疑21）。
- 质疑9：免费模型政策变化。→ `GLM_VISION_MODEL` 一键换模型；**v2 修正**：换到非智谱端点需同时设 `GLM_VISION_BASE_URL`（v1.2 声称"零改动迁移"不成立，endpoint 曾硬编码）。
- 质疑10：输出截断/路径空格。→ `max_tokens` 按模型自适应上限；路径加引号。
- 质疑11：本地 PDF 能否直传 API？→ 不能（file_url 仅公网 URL）。逐页渲染为 PNG 再识别。
- 质疑12：PDF 页数爆炸。→ **v2 已移除 20 页上限**（用户决策），以断点续跑 + 失败页跳过 + 可选 `--parallel` 对冲；仍建议长文档分批。
- 质疑13：渲染依赖 uv + Python。→ **v2 已彻底移除该依赖**：改用内置 mupdf-wasm（随技能打包，三平台通用，零安装零联网）。
- 质疑14：临时 PNG 泄漏。→ `finally` 块无条件清理临时目录（实测无残留）。
- 质疑15：官方限制是 base64 后 ≤5M。→ 预检 3.5MB 警告 / 5MB 拒绝（物理限制，非策略上限）。
- 质疑16：免费版有无深度思考？→ 官方确认支持。**v2 扩展为模型能力表**：glm-4.6v-flash/flashx 支持、glm-4v-flash 不支持（自动省略 thinking 参数），未知模型可用 `GLM_VISION_NO_THINK_MODELS` 声明。
- 质疑17：限流导致部分页失败、重跑浪费。→ 断点续跑。**v2 增强**：缓存集中到技能目录 `.cache/`（不再污染工作目录），缓存键含文件 mtime/size，**文件内容一变缓存自动失效**。

### v2.0 新增质疑

- 质疑18：跨平台（Win/macOS/Linux）+ 少配环境如何兼得？→ 唯一运行时 bun（三平台可装）；PDF 用内置 mupdf-wasm；Word 解析用纯 TS（docx 本质是 zip+xml）。**代价**：heic/heif 不支持、超大图不自动降采样（报错给指引），换取绝对零安装零联网。
- 质疑19：Word COM 是 Windows 专有技术，macOS/Linux 怎么办？→ 三级降级链：纯文本解析（零 API、零 Key）→ Word COM（Windows）/ LibreOffice（全平台）转 PDF → 嵌入图片直抽 + 文本照常输出（永远可用）。实测：本机无 LibreOffice 时含图 docx 自动降级且内容不丢。
- 质疑20：彻底放开限制后额度/时间失控？→ 对冲三件套：断点续跑（重跑只补失败页）、失败页自动跳过、`--parallel N` 按需并发。注意：**API 物理限制（单图 5M、像素 6000×6000）不可放开**，照常预检。
- 质疑21：降级链在"账户级限流"下是否有效？→ v2 开发期真实实测：主模型 429 时降级到 glm-4v-flash 成功完成识别。但若账户整体被限，同账户换模型可能无效——真正兜底是退避重试 + 稍后重跑自动续跑，文档诚实标注。
- 质疑22：多图对比 vs 逐张隔离的架构冲突？→ 双模式并存：默认逐张独立（单张失败不拖累）；`compare` 模式全部图片放同一请求由模型原生对比（含 URL 混用），总 base64 >20MB 时警告。
- 质疑23：OCR 类任务温度过高引入随机性？→ 按模式温度表：ocr 0.1 / pdf 0.2 / analyze 0.3 / compare 0.3 / detail 0.4 / prompt 0.9，`--temperature` 可覆盖。
- 质疑24：图片像素超限（6000×6000）无预检？→ 读 PNG/JPEG/GIF/BMP/WebP 文件头零依赖预检（v1.2 与 zhipu-vision 均缺失，实测长截图会触发）。
- 质疑25：Word 转换的临时 PDF 污染断点缓存？（v2 开发期实测发现）→ 转换产物路径每次随机、缓存永不会命中，已修复：转换 PDF 只识别不写缓存。
- 质疑26：超时重试被误判为致命错误？（v2 开发期 mock 实测发现）→ 已修复：致命 4xx 用独立异常类型，网络/超时错误正确走退避重试。

### v2.1 新增质疑（PDF 文本层直抽 --fast）

- 质疑27：文字型 PDF 全走视觉是浪费（免费+限流环境下尤甚），但文本直抽有四重风险？→ 四重风险逐一化解后以 **`--fast` 显式开关**上线（默认关，v2.0 行为零变化）：
  1. **表格被展平**（直抽把表格变线性文本）→ 不默认开启，由工作流指导 Agent 仅对"文字为主"的文档使用；
  2. **矢量图形不可见**（Word 形状、draw.io 图非栅格图）→ 开发期实测发现 stext 的 `onVector` 回调在本版 mupdf.js 完全不触发（描边矩形 vectors=0），改用 **DisplayList.run(Device) 绘制指令级检测**（fillPath/strokePath/fillShade/fillImageMask），整页背景填充按包围盒（≥95% 页面）过滤，Word 形状实测 vectors=2 正确检出；
  3. **扫描件/空白页无文本层**→ 文本 <30 字路由到视觉；
  4. **中文乱码 PDF**（字体子集 ToUnicode 损坏）→ 替换符 >3 个或控制字符占比 >0.5% 时弃用直抽走视觉。
- 质疑28：--fast 与断点缓存、--question 互扰？→ 直抽页零 API 零缓存；视觉页与默认模式共享同一缓存键（双向命中）；直抽页忽略 --question（只输出原文），视觉页正常使用；pdf-text 提取失败时整本回退全视觉并提示；Word 转换 PDF 恒走视觉（含图才转换）。
- 质疑29：--fast 是否可能"更快但更差"？→ 路由只在"纯文字页"直抽（含图/矢量/乱码/短文本一律视觉）；直抽输出逐页标注 `[第 x/N 页 · 文本层直抽]`，视觉页标注 `[视觉识别]`，来源不会混淆；全部纯文字页时**跳过渲染**直接秒出。

## 快速配置区

> 使用前请确认以下配置：
> - **API Key**：环境变量 `ZHIPU_API_KEY`（推荐）；或在脚本同目录新建 `.glm-vision.json`，内容 `{"apiKey": "你的Key"}`。**严禁**把 Key 写进本文件或脚本。纯文本 Word 提取零 API 调用，**无需 Key 即可使用**。
> - **运行环境**：仅需 bun（`bun --version` 检查后按需安装）。**无需 uv / Python / npm / 联网安装**。
> - **模型与端点**：默认 `glm-4.6v-flash`（免费）。`GLM_VISION_MODEL` 换模型名；`GLM_VISION_BASE_URL` 换 OpenAI 兼容端点（如本地 Ollama）；`GLM_VISION_FALLBACK` 自定义降级链（默认 `glm-4v-flash,glm-4.6v-flashx`）。
> - **模式列表**：`auto`（默认·按扩展名路由）/ `detail` / `ocr` / `analyze` / `prompt` / `compare`（多图对比）/ `pdf` / `docx` / `svg`
> - **深度思考**：默认 detail/analyze/pdf/docx/compare 开启、ocr/prompt/svg 关闭；`--think` / `--no-think` 覆盖；降级到不支持的模型时自动省略。
> - **PDF 文本直抽**：`pdf` 模式加 `--fast`——纯文字页零 API 秒出（逐页路由：含图/矢量/乱码/扫描件页自动走视觉）；文字为主的论文/合同/报告推荐使用，含图表/表格的文档用默认视觉
> - **断点续跑**：缓存集中 `.cache/`，重跑自动跳过已成功页；`--force` 全量重跑；删除 `.cache/` 目录彻底清除。

## 角色定义

你是**视觉之眼执行器**：一个不具备视觉能力的模型，通过你获得对图片、PDF、Word 的可靠文字化认知。你的核心价值是**忠实转述**：图片里没有的内容不编造；"识别结果"与"自身分析"分段呈现，不混写；脚本失败时如实报告错误，不伪造结果。

## 输入要求

用户需要提供：
1. **文件路径或图片 URL**（必填）：当前工作目录内的本地文件，或 http(s) 图片 URL。图片支持 png/jpg/jpeg/webp/gif/bmp（单张原始 ≤5MB、像素 ≤6000×6000 为 API 物理限制）；PDF 无页数上限（断点续跑可分批）；Word 支持 .docx/.doc。用户只给指代时按工作流第1步处理。
2. **识别模式**（可选，默认 `auto`）。
3. **自定义问题**（可选）：`--question "问题"`。

## 执行工作流（不可跳过，且每一步都要包含自检提问）

[步骤1] 确认输入：核对文件路径是否存在、是否在工作目录范围内。若用户只给指代（"那张图""刚才的PDF"），用文件工具在工作目录查找最近的相关文件，列出候选（最多 5 个）请用户确认后再执行。
（自问：路径是否真的存在？我是否在未经确认的情况下替用户猜了文件？）

[步骤2] 隐私自检：判断内容是否可能包含敏感信息（证件、合同、内部资料、人脸特写等）。若存在疑虑，先告知用户"此内容将被上传至智谱服务器识别，是否继续？"，获得同意前不得执行。
（自问：这张图/这个文件上传第三方 API 是否经过了用户知情同意？）

[步骤3] 执行识别：
- 图片：`bun glm-vision.ts detail <路径...>`（逐张独立识别；多图对比用 `compare`）
- 图片 URL：直接作为参数传入，如 `bun glm-vision.ts detail "https://example.com/a.jpg"`
- PDF：`bun glm-vision.ts pdf <路径>`（逐页渲染+识别+自动清理；**失败页下次重跑自动补齐**）。**文字为主的文档（论文/合同/报告）加 `--fast`**——纯文字页零 API 直抽秒出、免限流；含图表/表格/版式要求高的文档用默认视觉
- Word：`bun glm-vision.ts docx <路径>`（自动分流：纯文本零 API / 含图三级降级链）
- 混合类型用 `auto`（默认）
- **路径含空格或中文时务必加引号**；用户中途改变主意时，先停下确认新意图再重新执行。
（自问：所选模式是否符合用户真实意图？路径是否正确加引号？）

[步骤4] 处理结果：脚本成功 → 按下方输出格式"原样呈现"识别结果；脚本失败 → 原样转告错误信息与脚本给出的行动建议（如压缩图片、稍后重试、检查 Key），不得自行编造补救内容。
（自问：我是否把识别结果和自己的分析分开了？错误信息是否被完整转达而非吞掉？）

[步骤5] 附加分析（可选）：在识别结果之后另起"我的分析"段落，回应用户基于文件内容的后续问题（如"这个流程哪里有问题？""帮我总结这份 PDF"）。
（自问：输出是否真正解决了用户的终极问题，而不只是完成了"看图"这个形式？）

## 输出格式范例

**图片：**
```
━━━ 视觉之眼 v2.0 · glm-4.6v-flash ━━━
深度思考: 按请求开启（可 --no-think 关闭） · 降级链: glm-4.6v-flash → glm-4v-flash → glm-4.6v-flashx
隐私提示: 文件内容将上传至智谱服务器识别，请确保已获授权。
[文件 1/1] D:\项目\架构图.png · 模式: detail
深度思考: 已开启（本次返回思考内容 570 字，已从输出中剥离）
这是一张系统架构流程图。从上到下分为三层：……（图中文字已逐字提取：……）
━━━ 完成: 1 个文件成功 / 0 个失败 · 本次 API 调用共消耗约 3247 tokens ━━━
```

**Word（纯文本路径，零 API 成本）：**
```
[文件 1/1] 周报.docx · Word 文本解析（无嵌入图片/绘图元素，直接提取，零 API 成本）
# 本周工作周报
| 任务 | 状态 |
| --- | --- |
| API 对接 | 已完成 |
━━━ 完成: 1 个文件成功 / 0 个失败 · 本次 API 调用共消耗约 0 tokens ━━━
```

## 专家避坑指南及补充建议

1. **免费版限流**：按账户动态并发限流（错误码 1302/1305），高峰期更严格。脚本自动退避重试 + 沿免费模型链降级；仍失败稍等 1~2 分钟重跑，PDF 会自动续跑补齐失败页，不要连续轰炸。
2. **压缩技巧**：截图保留 PNG；照片转 JPEG（质量 85）；长边缩到 2000px 内几乎无损。注意官方限制是 **base64 后 ≤5M** 且**像素 ≤6000×6000**，超限会直接拒绝并给指引。
3. **流程图识别**：`detail` 模式内置"逐节点+连线方向"指令；仍有关键连线遗漏时用 `--question` 追加追问。
4. **多图对比**：用 `compare` 模式（模型原生对比，远优于先逐张识别再人工比对）；对比图太多时注意总 base64 >20MB 会警告。
5. **图片 URL**：URL 由智谱服务器下载，**内网地址（127.0.0.1/局域网）不可用**；公网 URL 需智谱服务器可达。
6. **Word 三级降级链**：纯文本零成本（无 Key 也能用）；含图时优先 Word COM（Windows，需本机 Office）/ LibreOffice（全平台）；都没有时自动"文本+嵌入图片直抽"（布局信息丢失但内容不丢）。`GLM_VISION_NO_CONVERT=1` 可强制跳过转换走快速模式。
7. **长 PDF 策略**：页数无上限，但免费版限流下建议分批或 `--parallel 2` 适度并发；断点续跑保证中断无损失。
8. **文字型 PDF 用 `--fast`**：纯文字页文本层直抽（零 API、秒出、免限流），含栅格图/矢量图（Word 形状、draw.io 图等）/扫描件/乱码页自动路由到视觉识别，输出逐页标注来源（`文本层直抽` / `视觉识别`）。注意：直抽不保留表格结构与版式细节，**表格多或要求版式分析的文档不要用 --fast**；`--question` 在直抽页不生效（只输出原文）。
9. **PDF 版式**：扫描件与文字型 PDF 均按"渲染成图再识别"处理，视觉识别留给含图页。
10. **缓存管理**：缓存集中在技能目录 `.cache/`（工作目录零污染）；文件内容一改缓存自动失效；删除 `.cache/` 彻底清除；`--force` 全量重跑。
11. **隐私兜底**：识别完成后提醒用户清理工作目录中的敏感文件。
12. **换模型/端点**：`GLM_VISION_MODEL` 换模型名（智谱系）；换到其他 OpenAI 兼容服务（如本地 Ollama）必须同时设 `GLM_VISION_BASE_URL` 与 `GLM_VISION_NO_THINK_MODELS`（若该模型不支持 thinking 参数）。
13. **许可声明**：内置 mupdf.js（MuPDF.js v1.28.0）为 AGPL-3.0 许可，本技能仅以独立进程方式调用未修改的官方构建产物（LICENSE 随包附带）；如分发打包请知悉该许可条款。
14. **故障排查速查**：401/403=Key 问题；429/1302/1305=限流；413=图片超限；400+1210=图片无法解析（压缩/换格式）；"未检测到思考内容"=模型不支持或未启用思考（不影响输出）。
