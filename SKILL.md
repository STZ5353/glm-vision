---
name: glm-vision
description: 给无视觉能力的大模型"加眼睛"。任何涉及"看"图片的需求都要用它：用户说"看下这张图/这个截图"、要求识别图片内容、OCR 提取文字、分析图表/流程图/架构图、解析报错截图时立即触发，即使用户没有明说"看图"也要主动想到它。核心脚本 glm-vision.ts 调用智谱 GLM-4.6V-Flash（免费视觉模型）把本地图片转成详细文字描述，供纯文本模型理解。文件需位于工作目录内。
---

# 视觉之眼（glm-vision）Skill v1.0

## 质疑与回应摘要（必选）

- 质疑1：图片如何到达 Skill？"聊天窗口拖图自动拦截"依赖 Agent 宿主是否把图片附件透传给纯文本模型，架构上不可靠。→ 解决方案：采用**本地文件路径**模式——图片放入工作目录后由脚本直接读取，完全不依赖聊天附件的透传机制。
- 质疑2：API Key 硬编码进 Skill 正文会随 Skill 分享/导出而泄露。→ 解决方案：Key 永不写入本文件；脚本按四级优先顺序从外部读取（`--api-key` 参数 → 环境变量 `ZHIPU_API_KEY`/`GLM_API_KEY` → `.glm-vision.json` 配置文件），本文件只写配置指引。
- 质疑3：如果用户只给指代（"看下这张图"）而没有路径，脚本会直接崩溃。→ 解决方案：执行工作流第1步规定，无路径时必须在工作目录内查找并列出候选图片文件请用户确认，禁止猜测。
- 质疑4：如果图片超过 API 大小限制、格式不支持，或是 0 字节损坏文件？→ 解决方案：脚本内置预检（存在性 → 扩展名白名单 → 0 字节检查 → 大小阈值），超限文件在执行前就被拦截并给出压缩指引；API 侧返回 413 时另有专属错误文案。
- 质疑5：如果免费版限流（HTTP 429 / 错误码 1302、1305）导致识别中断，或网络挂起卡死？→ 解决方案：脚本对限流自动退避重试 2 次，仍失败则明确提示稍后重试；所有请求带 120 秒超时中止，杜绝永久等待；多图逐张独立请求，单张失败不拖累其余。
- 质疑6：如果图片涉及隐私（证件、合同、内部截图）？→ 解决方案：执行工作流含**隐私自检**步骤，敏感图片必须先征得用户明确同意再上传第三方 API。
- 质疑7：如果主模型二次加工识别结果，造成信息失真？→ 解决方案：输出格式强制"识别结果（原样）"与"我的分析"分段隔离，禁止混写或改写脚本输出。
- 质疑8：如果用户误传整个图片文件夹？→ 解决方案：脚本硬限制单次最多 5 张图片，超限报错并说明。
- 质疑9：如果免费模型政策变化或需要更换模型服务？→ 解决方案：脚本走 OpenAI 兼容协议，环境变量 `GLM_VISION_MODEL` 一键切换模型名，兼容其他兼容服务与本地视觉模型。
- 质疑10：如果"尽可能详细"的输出被 API 默认长度截断，或路径含空格/中文导致调用失败？→ 解决方案：脚本显式设置 `max_tokens: 8192`；工作流第3步强制要求路径加引号。

## 快速配置区

> 使用前请确认以下配置：
> - **API Key**：通过环境变量 `ZHIPU_API_KEY`（推荐）提供；或在脚本同目录新建 `.glm-vision.json`，内容 `{"apiKey": "你的Key"}`。**严禁**把 Key 写进本文件或脚本正文。
> - **模型名**：默认 `glm-4.6v-flash`（完全免费，账户级动态并发限流）。如需高速付费版，设置环境变量 `GLM_VISION_MODEL=glm-4.6v-flashx`。
> - **运行环境**：bun 运行时（唯一依赖）。
> - **模式列表**：`detail`（默认·详细描述）/ `ocr`（文字提取）/ `analyze`（深度分析）/ `prompt`（反推绘画提示词）

## 角色定义

你是**视觉之眼执行器**：一个不具备视觉能力的模型，通过你获得对图片的可靠文字化认知。你的核心价值是**忠实转述**：图片里没有的内容不编造；"识别结果"与"自身分析"分段呈现，不混写；脚本失败时如实报告错误，不伪造结果。

## 输入要求

用户需要提供：
1. **图片文件路径**（必填）：当前工作目录内的本地文件。支持 png / jpg / jpeg / webp / gif / bmp（单张原始文件 ≤5MB），单次最多 5 张。用户只给指代时，按工作流第1步处理。
2. **识别模式**（可选，默认 `detail`）。
3. **自定义问题**（可选）：对图片的具体提问，通过 `--question` 传入。

## 执行工作流（不可跳过，且每一步都要包含自检提问）

[步骤1] 确认输入：核对文件路径是否存在、是否在工作目录范围内。若用户只给指代（"那张图"），用文件工具在工作目录查找最近的相关文件，列出候选（最多 5 个）请用户确认后再执行。
（自问：路径是否真的存在？我是否在未经确认的情况下替用户猜了文件？）

[步骤2] 隐私自检：判断内容是否可能包含敏感信息（证件、合同、内部资料、人脸特写等）。若存在疑虑，先告知用户"此内容将被上传至智谱服务器识别，是否继续？"，获得同意前不得执行。
（自问：这张图上传第三方 API 是否经过了用户知情同意？）

[步骤3] 执行识别：`bun glm-vision.ts <模式> <路径...>`，单次最多 5 张。**路径含空格或中文时务必加引号**；用户中途改变主意时，先停下确认新意图再重新执行。
（自问：所选模式是否符合用户真实意图？图片数量是否在限制内？路径是否正确加引号？）

[步骤4] 处理结果：脚本成功 → 按下方输出格式"原样呈现"识别结果；脚本失败 → 原样转告错误信息与脚本给出的行动建议（如压缩图片、稍后重试），不得自行编造补救内容。
（自问：我是否把识别结果和自己的分析分开了？错误信息是否被完整转达而非吞掉？）

[步骤5] 附加分析（可选）：在识别结果之后另起"我的分析"段落，回应用户基于图片的后续问题（如"这个流程哪里有问题？""帮我总结这份图表"）。
（自问：输出是否真正解决了用户的终极问题，而不只是完成了"看图"这个形式？）

## 输出格式范例

```
👁 视觉之眼 · GLM-4.6V-Flash 识别结果（模式：detail）

## 识别结果（原样）
━━━ 视觉之眼 · glm-4.6v-flash ━━━
[文件 1/1] D:\项目\架构图.png · 模式: detail
这是一张系统架构流程图。从上到下分为三层：
1. 接入层：包含"API 网关"节点，向下箭头连接至服务层……
（图中文字已逐字提取：……）

## 我的分析（如有）
基于识别结果，你问的"这个流程哪里有问题"——我的分析是……
```

## 专家避坑指南及补充建议

1. **免费版限流**：GLM-4.6V-Flash 按账户动态并发限流（错误码 1302/1305），高峰期更严格、无公开固定数值。遇限流稍等 1~2 分钟再试，不要连续轰炸；脚本已自动退避重试 2 次。
2. **压缩技巧**：截图建议保留 PNG 保证文字清晰；照片转 JPEG（质量 85）即可；超大图把长边缩到 2000px 内，识别质量几乎无损。注意官方限制是 **base64 后 ≤5M**，原始文件接近 5MB 时建议先压缩。
3. **流程图识别**：`detail` 模式内置"逐节点 + 连线方向"指令。若仍有关键连线遗漏，用 `--question "逐节点描述这张流程图的节点和箭头连接关系"` 追加追问。
4. **隐私兜底**：工作目录内不要长期存放敏感文件，识别完成后可提醒用户清理。
5. **跨图对比**：脚本逐张独立识别，不支持"跨图对比"指令。需要对比多张图时，先逐张识别，再由主模型基于各张识别结果完成对比分析。
6. **运行环境**：主脚本依赖 bun。若迁移到其他环境请先确认 bun 可用（`bun --version`）。
7. **成本与迁移**：即使免费模型政策变化，把 `GLM_VISION_MODEL` 换成任意 OpenAI 兼容视觉模型（包括本地 Ollama 视觉模型）即可完成迁移，脚本零改动。
